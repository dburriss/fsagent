# xml-sections

Demonstrates rendering prompts with XML section tags instead of Markdown headings.

This example shows how to configure output formats using `SectionStyle.Xml` for structured XML-based prompts, optionally with custom tag name mapping. This is useful when targeting LLMs that parse structured XML more reliably than Markdown.

## Files

- `xml-sections.fsx` - Script demonstrating Markdown and XML output formats
